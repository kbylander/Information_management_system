<?php
$password = $_POST['userpassword'];
$email = $_POST['useremail'];

include '../ConnectDB.php';
include '../disconnectDB.php'
//check correct password

//check if user inserted anytyhing into password box
if(empty($password)) {
    echo "Insert a password";
}
else {
    echo "You have inserted a password";
}

//check correct email



//if all correct; start session
?>